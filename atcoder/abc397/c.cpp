#include <bits/stdc++.h>

// solved with python =)

int main() {
    int n;
    std::cin >> n;
    std::vector<int> a(n);
    std::map<int, int> freq;
    for (int i = 0; i < n; ++i) {
        std::cin >> a[i];
        freq[a[i]]++;
    }


}